# CDC Critical 行基线 —— gates.sh 第 6 项拿"行集合"与它比，新增配对即判红。
# 来源：build/frozen_r23_srcseen/cdc.rpt（**当前采纳为演示默认的那一版**，bit md5 18443ffd）。
# 格式：`<源钟>><目的钟> <端点数> <Unsafe 端点数>`，一行一条，顺序无所谓（脚本自己 sort）。
# 第三列是 **2026-09-25 r56 加的**（ISSUES #65）：第 6 项原来只比"配对集合 + 端点数提示"，
# 于是 V8-5 那条时延快照把 `bus_tog`/`hb_tog` 接在同一根发射触发器上（CDC-11）时，
# `clk_fpga_0>clkout0_1` 的 unsafe 从 1 长到 3 而门禁**一声不响** —— 落在已有配对上就看不见。
# 本文件第 14 行早就写了"也不许再长出第二个不安全端点"这句话，缺的只是把它变成判据。
# 只有"某一版被采纳为新的默认"时才允许改这个文件，改的时候在下面记一行是谁、为什么。
# 注：#24/#25 里 eth_rxc>clk_fpga_0 从 Critical 掉到 Warning —— **06:1x 已查清（U14 结案）**：
#     那一行的配对并没有消失，是**它唯一那个 Unsafe 端点被删掉了**：
#       #23: Critical  Endpoints 272  Safe 271  Unsafe 1  Unknown 0  No ASYNC_REG 14
#       现在: Warning  Endpoints 271  Safe 271  Unsafe 0  Unknown 0  No ASYNC_REG 14
#     #24 的仲裁改造把 axi 域里那级 `eth_mode = 3FF(eth_link)` 删了（改成 src_arb 在 axi 域内
#     直接算 owner_eth），少掉的就是那 1 个不安全端点 ⇒ 工具把整行降级为 Warning。
#     所以这一条**可以记成改进**（有机制、有前后对照的行），但基线里仍保留它，
#     目的是继续钉住"这 271 个端点的配对不许悄悄长回来"。
#     但基线这一行**仍然保留 272**：门禁每次提醒"基线里有而本版没有"是有用的锚 ——
#     它钉住的是"这个配对的端点数不许长回来、也不许再长出第二个不安全端点"。
#     从今天起这条提醒是**已解释的已知差异**，不是没对上的账（见上面六行）。
eth_rxc>clk_fpga_0 272 1
clk_fpga_0>clkout0_1 17 1
eth_rxc>clkout0_1 51 1
sys_clk>eth_rxc 1909 2
