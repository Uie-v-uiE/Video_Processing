# CDC Critical 行基线 —— gates.sh 第 6 项拿"行集合"与它比，新增配对即判红。
# 来源：build/frozen_r23_srcseen/cdc.rpt（**当前采纳为演示默认的那一版**，bit md5 18443ffd）。
# 格式：`<源钟>><目的钟> <端点数>`，一行一条，顺序无所谓（脚本自己 sort）。
# 只有"某一版被采纳为新的默认"时才允许改这个文件，改的时候在下面记一行是谁、为什么。
# 注：#24/#25 里 eth_rxc>clk_fpga_0 从 Critical 掉到 Warning，原因未查证（OVERNIGHT_LOG §10 U14），
#     所以这里**保留**它 —— 保留它会让每次门禁都提醒一句"基线里有而本版没有"，
#     这正是要的效果：那笔没对上的账别想悄悄翻篇。查清之后再来修订本文件。
eth_rxc>clk_fpga_0 272
clk_fpga_0>clkout0_1 17
eth_rxc>clkout0_1 51
sys_clk>eth_rxc 1909
