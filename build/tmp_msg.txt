r53 上板并冻结：V8-8 手动缩放（八档 + 交还不瞬移），命令电池第一次跑就抓到自己留的痕迹

做的事：
- PL：`zoom_ctrl` 加八档表与手动档（0.25/0.33/0.50/0.75/1.00/1.33/1.50/2.00 ×，Q8 倒数尺度）。
  **0.25 档是 1023 不是 1024** —— inv_scale 只有 10 bit，1024 会回绕成 0（症状是"选最小档变成最大"）。
  手动只在帧首生效；**手动切回自动不许瞬移**：老写法在 inv 已在呼吸带之上时会一帧写成 INV_HI
  （1023→512 = 画面弹掉半幅），改成朝带回里走（每帧一步，实测 256 步进带）。
- 跨域：新增的两个位（[28:26] 档号、[29] 手动）**并进 effect_ctrl 已有的那条 sel 链**（9→13 位），
  不新开一组 ⇒ cdc.rpt 的配对集合不变。逐行归因（`frozen_r53_zoom/cdc_details.rpt`）：
  端点 67→71 全是 `axi_gpio_2` 31→35，Critical 仍 3 行、形状与 r52 相同。
- PS：`zoom <倍率>` / `zoom auto` 落地（整数解析，不引浮点），CFG_DATA0 整字写入，
  `[CTRL]`/`[STAT]` 报出 `zsel`/`zman`；开机探针顺带验那个通道真的是 32 位
  （图案放在保留段 [23:9] —— 探针不许命令硬件）。
- 判据：`sim/tb_v94_zoom_sel.v`（12 条，L1 从 57 变 **58/58**）；命令电池 37 → 43 条。
  两处语义问题被这次电池抓出来：`zoom 1` 沿用 V7 是**开关**不是 1.0 倍（ISSUES #63，不改老接口、
  改成拒绝消息自己说出来）；`zoom auto` 不改存好的 `zsel` ⇒ 末态 zsel=7 ≠ 初态 4 被判红
  —— **修的是电池（补 `zoom 1.0` 还原），不是放松判据**。
- 判据自己的判据：`uart_cmd_check.mjs` 加 `--align`（表与命令逐行对位，正例 + 插一条的反例）与
  `--replay`（离线复判旧捕获），并补一条哨兵：`[STAT]` 元组正则抓不到时两条都会是 'NO_MATCH'
  而"相等"，**那是永远红不了的判据** —— 现在明确报"判据本身过期了"（旧捕获即它的反例）。
  凭据 `build/battery_selfcheck_r53.txt`。
- 工具：`sim/run_one.sh` 的清单改走 `xvlog -f` + `cygpath -m`（58 个台架把 Windows 命令行撑爆，
  报的是没有 ERROR 字样的"参数太多"，最后伪装成 "Cannot find design unit"）。

数字：门禁 14/14 绿（WNS +0.487、WHS +0.054、失败端点 0、BRAM 65.71 %、LUT 20.79 %、
methodology/8-689/8-685x 全 0、顶层接线 534 条宽度可比 0 违规）；L1 58/58；
板级 43 条电池全过并回到初态、lane24 屏上 Latency 与回读同源 8/8（tot 4.06…15.7 ms）、drop_words 全程 0。
WHS 比 r52 的 +0.019 好，但本版没为它做任何动作 —— 记成"这一版够用"，不记成"修好了"，仍是深度优化第一优先。

三件套按 md5 冻结：bit 096f976d / xsa 7e5b39da / elf bd42a2d4（`build/frozen_r53_zoom/MANIFEST.md`）。
待：`board/README.md` 第 18 行的眼睛判据；"寄存器真的走到像素域"这一跳目前无机器凭据（`status` 没人读），
排任务 #45（lane23），与 #43 异常原因/温度的 lane 改动合成下一次构建。
