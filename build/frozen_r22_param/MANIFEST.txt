# build#22（R27：build#21 + P0-C ③ 参数集中化）—— 现在默认下这一块
# 门禁（bash build/gates.sh build/frozen_r22_param 七项全绿）：
#   WNS +0.566 / WHS +0.044 / 0 失败端点(23612) / BRAM 90.5 tile(64.64%) /
#   Slice LUT 7396(13.90%) / Reg 5903 / Dynamic 2.180 W / 0 布线错误 /
#   methodology 0 CRITICAL / cdc Critical 行 4（与 #17~#21 同一组，不新增）
# 内容（相对 #21）：**只是把 512/300/0x1000_0000/5001 从"两个例化各写字面量"收成一组 localparam**
#   （`src/rtl/top/system_top.v`）。值一个没改，行为按构造相同；L1 43/43；ModelSim vlog 0/0。
#
# ⚠ 这一版最有价值的产出其实是这条**自我纠正**：
#   我在提交那笔重构时写"期望 bit 与 #21 逐字节相同，若相同即证明是中性重构"。**不相同**
#   （#21 `f39e2e78` vs #22 `43b76e15`），而且连资源都动了：LUT 7452 → 7396（−56）、
#   WNS +0.770 → +0.566（−0.204）、Reg 5906 → 5903、端点 23615 → 23612。
#   ⇒ 结论：**改的只是"常数写在哪"，工具看到的名字/层次就变了，布局布线的选择也随之变** ——
#     所以"逐字节相同"根本不是一个合理的中性判据，我把一个可能的、但不可靠的证据当成了判据。
#   ⇒ 正确的中性判据是这三条，它们全过：① 值相同时的源码可核查（源码 diff 只有别名替换），
#     ② L1 全量 43/43，③ 门禁七项全绿。bit 哈希不在里面。
#   ⇒ 顺带得到一组干净的噪声样本：**逻辑完全等价**的两次实现，WNS 相差 0.204 ns。
#     这是"单次构建不能证明时序收益"这句话最硬的一个数据点（前面几次至少还有真实改动，
#     这一次连改动都没有）。已回填 `report/ISSUES.md` #39 与学习文档 §6.5。
#
# 为什么仍采纳 #22 而不是回退到 #21：**主线默认必须是"由当前源码构建出来的那一块"**。
#   仓库现在的源码含这笔重构，若默认 bit 是 #21，就等于把"bit ↔ 源码对应"这条纪律破掉。
#   #21 仍在回退链里（功能等价，md5 `f39e2e78`）。
# 回退链：#21(f39e2e78) → #19(545a27a1) → #18(534f7760) → #17(11998af8) → #13(0f46ec91)，实体都在盘上。
# 上板：与 #21 同一套检查（§9.5 全部步骤），因为行为没有差别 —— 这一笔**不需要**专门的板级验证。
