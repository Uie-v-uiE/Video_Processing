r45 冻结集 —— 2026-09-24 19:4x（V8-2：五级效果链 + 第二个控制字）
==================================================================

成套三件（换任何一件都要重新校验另外两件）：

| 文件 | md5 | 说明 |
|---|---|---|
| `system.bit` | bb301595 | 新窗口级 `proc_morph`（腐蚀/膨胀）、`proc_sharpen`；`proc_binary` 加判决极性；链按 spec 编号重排成五级；BD 加 `axi_gpio_2`（双通道 32bit，钉 0x41220000）；PS7 开 MIO GPIO（那两个 PS 按键电气上可达，**固件还没读它们**） |
| `system.xsa` | 80d9c316 | 与 bit 同一次构建；`ADDR_LOG` 三行数值比对通过（0x41200000 / 0x41210000 / 0x41220000） |
| `ps_app.elf` | f64ab316 | V8-1 命令层 + 九位算法字写到 0x41220000；开机做"写非零图案再读回"的 elf/bit 配套自检 |

门禁（`gates_r45.txt`）：WNS **+0.716** / WHS **+0.048** / 0 失败端点（27371）/
LUT 10150(19.08 %) / Reg 7624 / BRAM 65.36 % / 2.175 W / methodology 0 CRIT / cdc Critical 3 行 = 基线。
⚠ **WHS 从 0.050 掉到 0.048**：两个新窗口级都压在像素域，这条是"最小余量"，
以后每加一级像素域逻辑都要点名看它（`study/04_版本演进/03_优化的方法论.md` §7b 第 3 条）。

凭据：
- `regression_v79_r45b.txt` —— L1 全量 **`SIM DONE pass=51 fail=0`**（新增 `tb_v84_morph` 12 条、
  `tb_v85_sharpen` 7 条、`tb_v86_pipe_sel` 14 条）。
- `uart_cmd_check_r45.txt` —— 串口电池 **36 条全 ok**，含三条必须被拒的反例与"跑完回初态"；
  其中 `01010 → sel=030`、`pipe 000001010 → sel=0A0` 这两条同时是"老五位→新九位"映射的判据。
- `uart_r45_banner.txt` —— 开机自检 `[CFG] axi_gpio_2 @41220000 ok`（elf/bit 配套关系的机器验证）。
- `ddr_churn_r33_newelf.txt` —— #53 的绿边凭据（并发时 ETH 两个 bank 60/60 干净）。

这一版**改变观感**的两处（都需要眼睛，登记在 `board/README.md` 肉眼确认表 10~12 行）：
1. 效果链重排 + 多两级：`pipe 000100000` 锐化、`pipe 000001010` 二值化+腐蚀、`pipe 000001001` 膨胀。
2. **顶层总延迟从写死的 7 改成实测 15** ⇒ 右窗相对左窗会平移 8 个像素；
   图卡网格跨过左右分界处应当**连续**（以前是错 2 个像素）。见 ISSUES #54。

还欠的（不许在这版里算"已验证"）：
- ISSUES #55：长按反馈缺失 + 短按在按下沿发火 + `have_src` 用"链路活着" —— 三条修法排在 r46。
- ISSUES #54 的另一半（窗口中心与 Valid 对齐）必须在 V8-4 之前做完。
- PS 那两个按键：本版只把硬件通路打开，固件还没读（BSP 里没有 `xgpiops`，要么加驱动源码要么直写寄存器）。
- `rot/split/gamma/osd` 四类命令仍是"语法已收、硬件未接"。

回退：上一套完整可演示的是 `build/frozen_r44_v8cmd/`（bit `c7f90cc` + elf `1be21dee`）；
再往前是 `build/frozen_r32_sdfix/`。**校验**：`cd build/frozen_r45_v8morph && md5sum -c MANIFEST_BODY.txt`。
