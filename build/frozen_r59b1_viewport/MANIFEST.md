# r59b-1 冻结件（single viewport）— 2026-09-25 17:47 构建 / 17:51 板级复验全绿

一句话：**整屏变成一个视口** —— 一条地址流、一份源坐标、两个抽头（链前/链后）在同一级相遇，
原图那一路过 `raw_line_delay(LINES = u_pipe.OFF_LINES)` 的行环。这是"分割线 0~100 % 可调"与
"两幅图整体旋转缩放"两条主张共同的前提（#62/#73）。缝本身**仍钉在 512**，交给 #51 的 `split_ctrl`。

## 三件套（与下面每一条数字同一次构建 #45）
| 件 | md5 |
|---|---|
| `system.bit` | `ccbe9979` |
| `system.xsa` | `e5cc51d7` |
| `ps_app.elf` | `ff37ebe6`（与 r59a/V8-9 同一份：本轮没动 PS 侧） |

## 门禁 14 项 `GATES: ALL PASS`（`gates_r59b.txt`）
WNS **+0.575**（r59a 是 +0.262；本轮重排后反而变好，原因未查证 ⇒ 只记数不写"优化"）、
WHS **+0.053**、失败 setup/hold 端点 0/0、
**BRAM 96 tile / 68.57 %**（r59a 92/65.71 % ⇒ 行缓存加宽实测 **+4 块**，#73 里预估的是 +8）、
**LUT 12380 / 23.27 %**（r59a 11143/20.95 %）、Dynamic **2.199 W**、methodology CRIT 0、
布线错误 0、`cdc.rpt` Critical 3（基线 4）、端口宽度警告 0、多驱动 0、
顶层接线 violations 0。

## 台架（`l1_r59b.txt`、`r59b_x.out`）
`SIM DONE pass=64 fail=0`。顶层台架 `tb_v98_top_seam` 本轮新增并全绿的三条：
- **C1g** 源列 == `x_d[2]>>1`：**826259 格 0 不符**（级数由标定表实测得出 k=2 唯一，
  表就打在日志里 —— 不靠推理，我在这条上推理错过一次）；
- **C1h** 源行 == `(y_d[2]+u_pipe.OFF_LINES)>>1`：**0 不符**，帧底夹紧的 8334 格作为例外跳过并打印数量；
- **C0f** 显示帧缓存 38400 个字下标去重后全部被写过。
仍只报数的：C1b/C1c（内容逐格 = 标签）。原因这轮定位到了层级：
`X 分层：有效拍 3072000 | fb_rd 是 X 的 2150472（70 %）| fb_pix_hold 2152272 | fb_out 2150472`
⇒ X 来自 **BRAM 读出这一侧**，不是混色 mux、不是 skid（#54 的下一步按这个查）。

## 板级（`r59b_board_console.txt`）
仲裁八条全过；串口命令电池 **63 条全过且回到初态**；`RESULT board_verify PASS（判红的步骤：0）`
（这一行从 #69 起才是真总判定）。

## 还欠的眼睛判据（`board/README.md` 第 23 行，本轮唯一没闭合的一条）
① 画面应当**比原来大一倍**（一幅铺满 1024）；② 旋转/缩放**左右两半一起动**（旧行为是只动右半）；
③ 横方向模糊/边缘略宽是 #73 记过账的代价，不是错位；④ 中线两侧内容必须连续（缝仍在 512）。
