# build#21（R26：build#19 + ISSUES #38 收包链上顶层）—— 现在默认下这一块
# 门禁（bash build/gates.sh build/frozen_r21_rx 七项全绿）：
#   WNS +0.770 / WHS +0.027 / 0 失败端点(23615) / BRAM 90.5 tile(64.64%) /
#   Slice LUT 7452(14.01%) / Slice 2978(22.39%) / Reg 5906(5.55%) /
#   Dynamic 2.183 W / 12861 根可布线网全布通、0 路由错误 / methodology 0 CRITICAL /
#   cdc.rpt Critical 行 4（与 #17/#18/#19/#20 同一组，**没有新增跨钟违规**）
# 内容（相对 #19）：两个板的收侧从厂商 udp_rx 换成 gmii_rx_mac（自算 FCS-32）+ udp_rx_parser（目的端口过滤），
#   frame_reasm.p_good 从 1'b1 变成真值 ⇒ stat_bad 活了；Z7 的 health 读回从此有可信的"坏包"数字。
#   发侧仍是厂商 udp_tx + crc32_d8（Z7 上 tx_start_en 恒 0，行为与今天一致）。
# 与 #19 的资源/时序对照（逐条来自两份冻结报告，不是估的）：
#   LUT 7185 → 7452（+267 = 收包那一对）；Reg 5802 → 5906；BRAM 90.5 **不变**（厂商 udp_rx 里那个
#     FIFO 被省掉，正好抵掉新增逻辑 —— 所以这一笔没有把 BRAM 往上推）；功耗 2.184 → 2.183 W。
#   WNS +0.598 → +0.770：这条**不要当收益报**，同一设计不同轮次摆过 ±0.4 ns（#17 +0.447 / #18 +1.002
#     / #19 +0.598 / #20 +0.462）。但有一个**结构性**观测可以报：最差路径还是同一条 OSD 链
#     （x_d_reg[11] → u_osd/b_reg），级数 27 → **26**、CARRY4 10 → **9** —— 这是 R25 那次
#     "字形译码改并行 case" 的账面效果，它在 #20 单次构建里没能证明，在这里被同一份报告格式量到了。
# 判据链：
#   sim/tb_v795_rx_fcs.v     —— FCS 那一级（5 条，含"量具自校"与反向断言）
#   sim/tb_v795_rx_chain.v   —— 成对（C1 好帧逐字节、C2 翻位必须闭合判坏、C3 错端口过滤、
#                               C4 短帧也必须闭合、C5 两帧连发计数各自归位）
#   L1 43/43                 —— sim/results/regression_v79_r30.txt
#   顶层接线本身没有台架例化 ⇒ 端口存在性由这次 synth/impl 通过来证明（口径见学习文档 §0）
# 板上还没验（这一步要人看）：
#   ① 正常推流：画面与 #19 无差别，health 的 bad 应稳定为 0；
#   ② "bad 会动"的证据目前只有台架 C2（现场没有可靠的注错手段），所以只能报"改前是死的、改后有判据"；
#   ③ 端口过滤进了顶层，但 stat_drop_filt 还没接可读寄存器 ⇒ 过滤生效只能从"喂错端口的包不再进帧"间接看。
# 回退链：#19(545a27a1) → #18(534f7760) → #17(11998af8) → #13(0f46ec91)，四块 bit 实体都在盘上。
