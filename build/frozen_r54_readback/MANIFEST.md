r54 冻结集 —— 2026-09-25 05:2x · V8-7 因果位 + 片上温度 · V8-8 的最后一跳（lane23）

这一版把 V8 清单里剩下两件**不需要用户拍板**的事做完：仲裁"为什么归 PS"的因果位（lane30 bit[10:8]）、
像素域**正在用**的缩放状态回读（lane23），加一条 PS 侧 XADC 的片上温度（PL 零改动）。
**新增了一对跨域**（lane23 的 19 位准静态总线 + 它自己的心跳/沿），
代价与判据都写在下面"门禁"一节与 `report/OVERNIGHT_LOG.md` §37。

## 成套三件（md5 前 8 位）

| 文件 | md5 | 说明 |
|---|---|---|
| `system.bit` | **e049c2b5** | build #35（`../r54b_build35.log`，`SYSTEM BUILD DONE`，0 ERROR） |
| `system.xsa` | **662c0d5f** | 与 bit 同一次导出 |
| `ps_app.elf` | **812790ec** | 新增 `xadc_init()` + `temp` / `temp th <°C>`；`[STAT]` 字段顺序未动 |

⚠ 上一版 **build #34**（同一个源码树、心跳还借用 `sof_tgl` 的那次构建；产物已被 #35 覆盖，未冻结）
门禁第 6 项**判红**：
lane23 的心跳借用了 `sof_tgl`，一个发射触发器扇出到两组目的域同步器 ⇒
`clkout0_1→clk_fpga_0` 整对被提成 CDC-11 Critical（27 端点 / 2 unsafe）。
凭据 `gates_r54_build34_CDC_RED.txt`；修法是给跨域一个自己的翻转触发器（`z_hb_tog`），
#35 里这一对退回 Warning ⇒ Critical 行集合与基线一致。**没有改基线、没有加豁免。**

## 门禁十四项：ALL PASS（凭据 `gates_r54b.txt`，当场跑）

| 项 | r53（#33） | r54（#35） | 阈值 | 判 |
|---|---|---|---|---|
| WNS | +0.487 | **+0.235 ns** | ≥0 | PASS |
| 失败 setup 端点 | 0 / 29403 | 0 / **29526** | =0 | PASS |
| WHS | +0.054 | **+0.012 ns** | ≥0 | PASS ⚠ 见下 |
| 失败 hold 端点 | 0 | 0 | =0 | PASS |
| BRAM | 92 / 65.71 % | 92 / 65.71 % | ≤97 % | PASS |
| Slice LUT | 11059 / 20.79 % | **11116 / 20.89 %** | ≤98 % | PASS |
| Slice 寄存器 | 8351 | **8430** | 记录用 | PASS |
| Dynamic | 2.182 W | **2.182 W** | 与前次同量级 | PASS |
| methodology CRITICAL WARNING | 0 | 0 | =0 | PASS |
| 布线错误网线 | 0 | 0 | =0 | PASS |
| cdc.rpt Critical 行 | 3（= 基线配对集合） | **3** | 不新增配对 | PASS |
| Synth 8-689 位宽警告 | 0 | 0 | =0 | PASS |
| 多驱动 net 8-685x | 0 | 0 | =0 | PASS |
| 顶层接线（名/悬空/**位宽**） | 546 比较 / 0 违规 | **546 / 0** | violations=0 | PASS |

⚠ **WHS +0.012 ns 不许当"通过"用**：§14 R21 补记里，+0.012 这个量级曾被明确判为
"换个温度/电压就可能翻"。这一版它比 #34 的 +0.006 好一点，但仍是史上最低档之一，
且 r51→r54 四版的 WHS 是 +0.048 / +0.019 / +0.054 / +0.012 —— 上下跳，说明它是**布局布线的运气**
而不是被修好的东西。**深度优化那一轮（D8 步 ②）的第一个数字就是它**，不改口径、不当绿灯。

## 板级实测（这一版第一次能机器判"屏上画的倍率 = 取数器在用的倍率"）

| 项 | 结果 | 凭据 |
|---|---|---|
| 三件套配套（elf↔bit） | `[CFG] axi_gpio_2 @41220000 ok` + `[CFG] gamma window @41220008 ok` | `uart_r54_boot.txt` |
| PS-XADC 起得来 | `[TEMP] PS-XADC @F8007100 ok` | 同上 |
| **lane23 缩放同源** | `alive=1 zsel=4 inv=454 → 实际 0.564x，屏上档 zcode=2=0.50x ⇒ verdict=OK` | ``build/evidence/verify_0925_0521.txt``（`health_read` 第 2 步） |
| `Latency:` 那一格仍同源 | `osd_ms_matches_tot = true` | 同上 |
| 入包链 | `drop_words = 0` | 同上 |
| **仲裁八条全绿（含新 V7）** | 交接 324 ms、交回 **235 ms**、再推 133 ms、稳占 29 点零翻转；V7：交回段 39 个样本原因位全部可用 | `arb_handover_last.json`（`data/measured/`）+ 本目录 `arb_selftest_r54.txt` |
| **串口电池 51 条全绿** | 含八条温度：`degC=61.42 raw=0xA9F3 vccint=998mv over=0 sane=1`；`th=0C → over=1`、`th=200C → over=0`、两条非法参数被拒 | `battery_r54.txt` |

电池第一次跑是 **50/51**，红的那条是我的判据把 `raw=` 写成小写十六进制，而 `xil_printf` 的字母表是大写
（`raw=0xA9F3`）。改的是**模式的字母**并要求恰好 4 位十六进制（更紧，不是更松），理由写在
`src/host/uart_cmd_check.mjs` 那张表的注释里。

## 这一版的台架账

| 台架 | 条数 | 变异对照（证明判据能红） |
|---|---|---|
| `tb_v796_src_arb`（G 段新增） | 全绿，G1 = **16 组激励 × 3 实例** | G2a/b/c 钉"原因必须先于换手"（从 `owner_eth` 反推为什么也过 G1、但过不了 G2） |
| `tb_v95_zoom_snap`（新） | 16/16 | 两次（`mutation_zoom_snap_r54.txt`）：**只**把 8 拍延迟去掉时，Z3/Z5/Z6 全绿、只有 Z4 红 ⇒ 时序类判据不是装饰 |
| `tb_v96_zoom_scan`（新，#56 第 1 点） | 12/12 | `mutation_zoom_scan_r54.txt`：`>>>8` 改成向 0 截断（只动负半边）⇒ 立刻找不到流水线对齐位移并红 |
| `tb_v97_seam_scan`（新，#56 第 2 点） | 9/9 | `mutation_seam_r54.txt`：去 `&& de` ⇒ 只红 S5；`oob` 恒 0 ⇒ 只红 S4；另用 `PANE_W=0/1/1024` 三个实例预检 V8-4a |
| `health_read --selfcheck` | 28/28 | S3 双向错移变异、S3 每个真实位都必须被抓住 |
| `arb_handover_test --selftest` | 13/13（原 10） | 新增两条专钉 V7：原因位说谎 ⇒ 必须红 |
| `temp_formula_check` | 全绿 | 三条变异（少移 4 位 / 忘减偏移 / 常数少一位）+ 容差是**推导**的（常数截断 4.5e-6 × 504 K + 两次取整） |

L1 全量：`SIM DONE pass=61 fail=0`（`l1_r54_console.txt`）。

## 还欠什么（都不欠机器的，欠的是眼睛/手/决定）

1. 屏上：SD 是否真的又动了（`board/README.md` 第 12–19 行的眼睛表，第 19 行是这一版新加的温度那条）；
2. 手动缩放钉住后画面真的不动、`Zoom:` 那格 `(Auto)` 真的消失、2x 无黑边 / 0.25x 有黑边、
   `zoom auto` 从当前倍数继续 —— lane23 已经能机器判"档号与 inv 自洽"，**画面上真是那张图**仍要眼睛；
3. 温度：`over` 两档与 `sane` 都过了，"这个数字随芯片温升爬"要用手；
4. #44（V8-4b 几何统一）等用户对演示语义拍板；V8-7 的"原因上屏"这一版**决定不做在 OSD 上**
   （用户的四行格式是逐字定的，且它要再付一次跨域）——理由与做法都写在 `report/PLAN_V8_SPEC.md` §7f 开头。
5. r55 已排好三件小事（`OVERNIGHT_LOG.md` §37 第 9 节）：删死掉的 16 位"两级当总线"同步器（ISSUES #64）、
   异步复位释放的同步、以及给两份新台架补变异（已顺手做完 v96/v97 那两个）。
