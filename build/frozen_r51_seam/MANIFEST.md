# r51 冻结件（可动分割线）— 2026-09-25 18:28 构建 / 18:52 板级复验全绿

一句话：**分割线从此真的可动**。`split_ctrl` 上顶层，缝位、自动扫、follow、swap、蓝线开关
都由串口说了算；OSD 第四行 `Split:` 那一格改由**执行值**驱动（不再是几何算出来的死数）。
这是 r59b-1（整屏单视口，`frozen_r59b1_viewport/`）之后 4b 的第二块，也是"0~100 % 可调"
这条主张第一次可以被机器复验。

## 三件套
| 件 | md5 |
|---|---|
| `system.bit` | `21637a10` |
| `system.xsa` | `3988a546` |
| `ps_app.elf` | `1e157e9f` |

## 门禁 14 项 `GATES: ALL PASS`（`gates_r51.txt`）
WNS **+0.097**、WHS **+0.017**、失败 setup/hold 端点 0/0、BRAM **96/68.57 %**、LUT **12793/24.05 %**、
Slice 寄存器 9422、Dynamic **2.207 W**、methodology CRIT 0、布线错误 0、
`cdc.rpt` Critical 3（基线 4，配对不新增、unsafe 不增长）、端口宽度 0、多驱动 0、顶层接线 violations=0。

⚠ **必须写下来的风险**：WNS/WHS 是本项目至今最薄的一档（上一版 +0.575/+0.053）。
新增的就是 `split_ctrl` 的三角波 + 端点乘法 + 缝比较进混色 mux 那一段。
**不许靠放松约束把它变绿**；下一次改动前先记住可用的两个杠杆：
① 把 `split_eff` 提前一拍寄存（它只在一行内变，晚一拍没有观感代价）；
② 端点乘法 `u*W` 是纯组合、可以折到帧首去算（每帧一次）。

## 控制位（`ISSUES #70 追加` 是唯一出处）
`CFG_DATA0[22:13]=pos_px（显示列 0..1024）`、`[23]=auto_en`、`[24]=follow`、`[25]=swap`、`[30]=marker_off`。
14 位在 axi 域每 ~1.3 ms 整拍抄一次并翻 toggle，经 `snap_cross` 落到像素域
—— **不并进 effect_ctrl 那条 ASYNC_REG 链**，因为 #71 量过：加宽它会让 unsafe 端点按位长涨、门禁直接判红。

## 台架 / 板级
`l1_r51.txt`：`SIM DONE pass=64 fail=0`（含 `tb_v97` 四实例把缝分别钉在 512/1/0/1024 全绿，
`r51_v97.out`；顶层台架 `tb_v98` 接了新输入 `split_ctl`，几何两条 C1g/C1h 仍 0 不符，`r51_v98.out`）。
`r51_board_console4.txt`：**仲裁九条全过**（V1 基线 0/8 + 新拆的 V1b"无流却占着"）、
命令电池 **71 条全过且回到初态**、`RESULT board_verify PASS（判红的步骤：0）`。

## 本轮顺手改掉的一条判据缺陷（值得单独记）
`V1 基线归 PS` 原来一条判两件事：基线里 owner=1 既可能是**外部真在推流**（18:36 那次就这么红了，
事后读板 `eth_live=0`、包数 6 秒不动 ⇒ 当时确实有别人的流），也可能是**没有流却还占着总线**（真故障）。
lane30 本来就有独立的 `eth_live` 位 ⇒ 拆成 V1（前提：静默）+ V1b（owner=1 且 live=0 必为零）。
**拆完 V1 的牙没变钝**：无流却占着照样红，只是不再把环境当硬件报。计数从八条变九条，
`board_verify.sh` 的绿色标记与退出码一起跟上（#69 之后它才有真退出码）。

## 还欠
`board/README.md` 第 23 行的眼睛判据（整幅铺满 / 旋转左右一致 / 缝两侧内容连续），
外加本轮新增一眼：`split 20` → `split 80` 慢慢推，看缝移动时**两侧内容是否跟着同一幅走**（不该出现横带或错位）。
