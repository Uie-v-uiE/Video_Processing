# exp_r20（R25）：OSD 字形译码由串行区间比较改并行 case —— 等价、省 LUT，但**不构成时序收益**，不采纳
# 内容：build#19 + 只改 osd_overlay.glyph_idx（真值表由 256 码点差分证明等价，sim/tb_v794_osd_glyph.v）
# 门禁（bash build/gates.sh build/exp_r20_glyph 全部 PASS）：
#   WNS +0.462 / WHS +0.066 / WPWS 记于报告 / 0 失败端点(23424) / BRAM 90.5(64.64%) /
#   Slice LUT 7100(13.35%) / Slice Reg 5802(5.45%) / Dynamic 2.182 W / 0 布线错误 / methodology 0 CRIT / cdc 4 行 Critical（与基线同）
# 对比 build#19：WNS +0.598 → +0.462（**低 0.136 ns**）、WHS +0.043 → +0.066、LUT 7185 → 7100（省 85）、功耗 2.184 → 2.182
# 最差路径两次都是同一条：u_pl/x_d_reg[11][x] → u_pl/u_osd/{b,g}_reg[y]/D，
#   #19：27 级、CARRY4=10、route 66.2%   |   #20：27 级、CARRY4=9、route 65.7%
# ⇒ 结论（这条是本实验最有用的产出）：**这一改没有把瓶颈的级数打下来**（27 → 27，只少一个 CARRY4），
#   所以 WNS 那 0.136 ns 的差别落在本设计已实测的轮次噪声里（#17→#18→#19 之间同一条链摆过 +0.447/+1.002/+0.598），
#   **不能拿一次构建说时序变好或变坏**。省下的 85 个 LUT 是真的（并行译码比"区间比较+减法"便宜）。
# 处置：**不作为明早默认**（默认仍是 build#19 / `545a27a1`，`build/system.bit` 已按 §7 的规矩恢复回去）；
#   RTL 改动**保留在主线**（它语义等价、省资源、hold 更好，且没有任何延迟变化），
#   但它不进任何"WNS 提升了"的说法里。
# 真要动这条链，需要的是**结构改动**而不是换写法：把 `chars[]` 变成真 RAM（按时序整片写入 + 组合读），
#   以及把 `line*MAX_CHARS+cidx` 的乘法换成 2 的幂列距（MAX_CHARS=10→16）或直接寄存地址；
#   这两条都会改延迟 ⇒ 必须同步动顶层 `PROC_LAT` 与配准判据（`sim/tb_osd_lines.v`、`tb_v5_vblast`），
#   并且要上板看文字位置 —— 那是白天的活，不适合凌晨盲改。
