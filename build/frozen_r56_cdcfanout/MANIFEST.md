# r56 冻结集 —— 2026-09-25 06:2x · 修掉一条"门禁看不见"的 CDC-11（ISSUES #65）+ 把那条盲区本身补上判据

一笔**只有 1 个触发器**的改动，但它做的是两件事：
① 关掉 #65 —— V8-5 的时延快照把 `bus_tog` 与 `hb_tog` 接在同一根发射触发器上，
造成 `clk_fpga_0 → clkout0_1` 这一对多出 **2 个 unsafe 端点（CDC-11）**；
② 让这种形状**下一次一定会红** —— 门禁第 6 项原来只比"Critical 配对集合"，
这条退化落在已有配对上所以看不见；现在基线扩成三列 `配对 端点数 unsafe`，unsafe 增长即判红。

## 成套三件（md5 前 8 位）

| 文件 | md5 | 说明 |
|---|---|---|
| `system.bit` | **c15454ae** | build #37（`r56_build37.log`，`SYSTEM BUILD DONE`，`grep -c "^ERROR"` = 0） |
| `system.xsa` | **6d69823c** | 与 bit 同一次导出 |
| `ps_app.elf` | **812790ec** | **与 r54/r55 逐字节相同**：PS 侧零改动，故意沿用 |

## 门禁十四项：ALL PASS（凭据 `gates_r56.txt`，且是在**新判据下**PASS）

| 项 | r55 (#36) | r56 (#37) | 判 |
|---|---|---|---|
| WNS (ns) | 0.235 | 0.540 | PASS —— **不许记账为改进，见下面那段** |
| WHS (ns) | 0.012 | 0.019 | PASS —— 同上 |
| 失败 setup / hold 端点 | 0 / 0 | 0 / 0 | PASS |
| BRAM | 92 / 65.71 % | 92 / 65.71 % | PASS |
| Slice LUT | 11116 / 20.89 % | 11108 / 20.88 % | PASS |
| Slice 寄存器 | 8430 | **8431** | PASS（**+1，正是 `lat_hb_tog`**） |
| 端点总数 | 29526 | 29527 | 记录 |
| Dynamic | 2.182 W | 2.182 W | PASS |
| methodology CRIT / 布线错误 / 宽度警告 / 多驱动 | 0 | 0 | PASS |
| **第 6 项 CDC** | 3 条 Critical 配对 | 3 条，**且 unsafe 不增长**（新判据） | PASS |
| 逐实例接线 | violations=0 | violations=0 | PASS |

**这一版真正要看的两行**（`cdc.rpt` + `cdc_details.rpt`，后者由 `cdc_who.tcl` 出）：

| 配对 | r23 基线 | r55 | r56 |
|---|---|---|---|
| `clk_fpga_0 → clkout0_1` 端点/unsafe | 17 / **1** | 71 / **3** | 71 / **1** |
| 全设计 CDC-11 行数 | — | 2 | **0** |

剩下的那 1 个 unsafe 是 **r23 就有的** CDC-10（`u_pl/u_cmt/d1_reg/C → u_pl/ac0_reg/D`，同步器前有组合逻辑），
另有两条 CDC-10 在别的配对上（含 `u_eth/u_reasm/stat_pkts_reg[9] → u_pl/el0_reg/D`，基线里也记着）。
⇒ 这一对回到了基线水平，**没有改基线数值、没有加豁免**。

⚠ **WNS/WHS 变好这件事不许写进任何对外材料**：一笔 1 个 FF 的改动碰不到全设计的关键路径
（那条是 OSD 算术，24 级 + 9 CARRY4），0.235→0.540 与 0.012→0.019 都是**布局扰动带来的运气**。
本仓库对这类事的口径一贯是：只有策略扫描 + 两次独立构建都同方向，才叫改进。
WHS 的历史（r51→r56：+0.048/+0.019/+0.054/+0.012/+0.012/+0.019）说明它在 0.01~0.05 之间来回跳，
**深度优化那一轮（任务 #46）的第一个数字仍然是它**。

## 判据链（每一条都点名凭据）

- **L1 全量 61 个台架 61 过 0 红**：`l1_r56_console.txt` / `l1_r56_xsim.log`（`SIM DONE pass=61 fail=0`）。
- **门禁第 6 项的新判据自带反例**：`gates_cdc_test_r56.txt`，5 个 case 全过 ——
  T1 用**真实的 r55 冻结件**证明会红、T2 把 unsafe 改回 1 证明**会绿**（否则就是永远红的假判据）、
  T3 新增配对、T4 基线少一列、T5 基线不存在。
- **`tb_v97_seam_scan` 的变异测试（§37 欠的那笔）还上了**：`mutation_seam_v97_r56.txt` ——
  把 `split_display.v` 的 `if (sep && de)` 故意改成 `if (sep)`，结果**只有 S5 红**、其余 8 条全绿；
  源文件随后对回原 md5 `0a874464`（还原与运行写在同一条命令里，防"忘了还原"）。
- **板级复验**（`r56_board_verify_console.txt`，日志副本 `verify_0925_0616.txt`；下载三段日志齐全）：
  串口活着且 SD 在放（`[SD] frame 400: last 100 frames 29.999 fps`、无 `play refused`、无 `[TEMP!]`）；
  `lane23 verdict=OK`（`alive=1 zman=0 zsel=4 zcode=3 inv_scale=424` ⇒ 实测 0.604×，屏上标 0.75x 是自动呼吸的当前档）；
  `lat.osd_ms_matches_tot=true`、`drop_words=0`；
  **仲裁八条全绿**（`arb_handover_r56.json`：接管 119 ms、稳占段 28/28 零翻转、停流交回 **468 ms**、
  交回后 0/39 不回跳、再推 176 ms 可逆、采样 100/117、原因位不可用 0 个）；
  **命令电池 51/51**（`battery_r56.txt`）且末态=初态（`en=00 thr=80 src=1 zoom=1 bilin=1 zsel=4 zman=0 gm=0.00`）。
  ⇒ 交回 468 ms 比 r55 的 227 ms 慢，但**在这条链已知的散布内**（r32 那次 9 个 run 量到 208~481 ms，
  采样周期本身就是 300 ms）⇒ 对外口径不变：**0.2~0.5 s，采样分辨率 300 ms**，不念最漂亮的那一个数。

## 板上现在跑的是 r56

重刷顺序（不能反）：`ps_jtag_boot.tcl` → `program_pl.tcl` → `ps_app_reload.tcl`。
演示默认**仍是 #23 那一版**（`frozen_r23_srcseen`），因为三条眼睛判据还没收口 —— 换默认是用户的决定。

## 还欠的

- 眼睛：`board/README.md` 行 12~20（其中 12 与 18 正好是用户报的 #56 两条现象）。
- 用户的三个表态：4b 几何语义（决定"分割线 0~100 %"这条 spec 硬要求）、原因位上不上屏、双线性要不要再开一晚。
- 步 7 的一条异常还没人为造过：**SD 播放中途拔卡**（拔线、非法命令都造过）。
- 任务 #46（深度优化时序/资源/功耗）与 D8 的 ③~⑥（文档重写、上新分支、联合验证、最终 main）**等用户发话**。
